# @app-speed/audit/domain

This lib is responsible for the definition of the audit domain models

```bash
pnpm exec nx run audit-domain:build
```

Test with:

```bash
pnpm exec nx run audit-domain:test
```
