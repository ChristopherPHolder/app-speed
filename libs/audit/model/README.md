# @app-speed/audit/model

Shared audit domain vocabulary.

This library provides:
- builder-facing defaults and step definitions
- shared device and Lighthouse step constants
- runtime user-flow types

Build with:

```bash
pnpm exec nx run audit-model:build
```

Test with:

```bash
pnpm exec nx run audit-model:test
```
