# s3-upload

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build s3-upload` to build the library.

## Running unit tests

Run `nx test s3-upload` to execute the unit tests via [Jest](https://jestjs.io).
