{
  "executors": {
    "s3-upload": {
      "implementation": "./src/executors/s3-upload/executor",
      "schema": "./src/executors/s3-upload/schema.json",
      "description": "Executor to upload files to an s3 bucket"
    },
    "ssm-deploy": {
      "implementation": "./src/executors/ssm-deploy/executor",
      "schema": "./src/executors/ssm-deploy/schema.json",
      "description": "Deploy API image to EC2 instances via AWS SSM document"
    },
    "ec2-ssm-cycle": {
      "implementation": "./src/executors/ec2-ssm-cycle/executor",
      "schema": "./src/executors/ec2-ssm-cycle/schema.json",
      "description": "Start EC2, execute SSM document, then stop EC2"
    }
  }
}
