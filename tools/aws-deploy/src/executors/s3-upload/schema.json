{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "S3Upload executor",
  "description": "",
  "type": "object",
  "properties": {
    "bucket": {
      "type": "string",
      "description": "S3 bucket to upload the content too"
    },
    "upload": {
      "type": "string",
      "description": "Path from root to file of directory being uploaded"
    }
  },
  "required": []
}
